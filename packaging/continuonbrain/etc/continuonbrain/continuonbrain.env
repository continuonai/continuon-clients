#
# ContinuonBrain environment overrides (packaged install).
#
# NOTE: Chat→RLDS logging is privacy-sensitive and is usually opt-in.
# This repository owner has explicitly requested it enabled by default for HOPE v1 training.
#

# Enable logging chat turns as RLDS episodes under: <config-dir>/rlds/episodes
CONTINUON_LOG_CHAT_RLDS=1

# (Optional) You may override the episodes directory explicitly:
# CONTINUON_CHAT_RLDS_DIR=/opt/continuonbrain/config/rlds/episodes

