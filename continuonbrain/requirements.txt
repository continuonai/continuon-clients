# Core Dependencies
transformers>=4.40.0
accelerate>=0.29.0
torch>=2.2.0
numpy>=1.26.0
opencv-python>=4.9.0

# Hardware / GPIO
adafruit-circuitpython-servokit>=1.3.0; platform_system == "Linux"
lgpio>=0.2; platform_system == "Linux"  # Critical for Pi 5 GPIO/I2C interaction (requires system packages: swig, liblgpio-dev)
smbus2>=0.5.0; platform_system == "Linux"  # Optional I2C helper (some boards/drivers use it)
adafruit-circuitpython-pca9685; platform_system == "Linux"
adafruit-circuitpython-motor; platform_system == "Linux"

# Depth camera (OAK-D / Luxonis) is optional and may not be supported on all Pi OS builds.
# Install it manually when needed (and only if it works on your platform):
#   pip install depthai --extra-index-url https://artifacts.luxonis.com/artifactory/luxonis-python-local/

# Utilities
psutil
requests
python-dotenv
