# Trainer UI - Web interface for Brain A training

# Core (required)
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# Camera & Face Recognition (optional but recommended)
opencv-python>=4.8.0
numpy>=1.24.0

# Face recognition (optional - requires dlib)
# Install dlib first: pip install dlib (may need cmake)
face-recognition>=1.3.0

# Note: On Raspberry Pi, use opencv-python-headless instead:
# pip install opencv-python-headless
