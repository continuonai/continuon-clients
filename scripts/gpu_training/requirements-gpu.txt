# GPU Training Requirements for ContinuonBrain
# Tested with CUDA 12.x on Windows

# JAX with CUDA support
jax[cuda12]==0.4.35
jaxlib==0.4.35

# Core ML libraries
flax>=0.8.0
optax>=0.2.0
orbax-checkpoint>=0.5.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.65.0
tensorboard>=2.15.0
psutil>=5.9.0

# For RLDS data loading
tensorflow>=2.15.0
tensorflow-datasets>=4.9.0

# Optional: Weights & Biases for experiment tracking
# wandb>=0.16.0

# HTTP for API calls (if connecting to Pi server)
requests>=2.31.0

