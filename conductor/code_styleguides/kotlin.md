# Kotlin Style Guide

- Follow official Kotlin coding conventions.
- Use `PascalCase` for classes and `camelCase` for variables/functions.
- Prefer immutable `val` over `var`.
- Use expression bodies for simple one-line functions.
- Documentation: Use KDoc for public APIs.
- Testing: Use JUnit 5 and MockK.
